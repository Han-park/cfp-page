import { getDictionary } from "@/lib/dictionary"; import { Locale } from '@/i18n.config'; import GitHubActivity from '@/components/GitHubActivity'; import ProjectsSection from '@/components/ProjectsSection'; import PageWrapper from '@/components/PageWrapper'; import { JSX } from 'react'; export default async function HomePage({ params: { lang }, }: { params: { lang: Locale }; }): Promise<JSX.Element> { const dict = await getDictionary(lang); return ( <PageWrapper lang={lang}> <ProjectsSection title={dict.home.title} /> <GitHubActivity /> </PageWrapper> ); }
